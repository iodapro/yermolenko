В срр-файлі програма для добутку двох квадратних матриць.

В таблиці ексель - залежність часу виконання програми (в секундах) від розміру матриці (N - кількість рядків/стовпчиків матриці).

При великих значеннях N виходить кубічна залежність часу від кількості рядків N (тобто t = O(N^3)).